name: Container Release

on:
  push:
    tags:
      - "v*"

permissions: {}

jobs:
  container-release:
    permissions:
      attestations: write
      contents: write
      id-token: write
      packages: write
    uses: berntpopp/genefoundry-router/.github/workflows/_container-release.yml@9bef2001f5610731033df9b6681b6fc60daf8591 # genefoundry-router container-release standard v1
