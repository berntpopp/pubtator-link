name: Container Release

on:
  push:
    tags:
      - "v*"

permissions: {}

jobs:
  container-release:
    permissions:
      attestations: write
      contents: write
      id-token: write
      packages: write
    uses: berntpopp/genefoundry-router/.github/workflows/_container-release.yml@915356ac10bb3542f379db612a14b140e3c81078 # genefoundry-router container-release standard v1
